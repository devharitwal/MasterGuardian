Just run npm install and then use sudo command to run with elevated privledges. 
There is a file that contains the list of most common harmful websites you can update the list or you can simply add more to the block list. 
